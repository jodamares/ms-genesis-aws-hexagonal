<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <springProperty name="applicationName" source="spring.application.name" defaultValue="ms-organization-template"/>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp/>
                <pattern>
                    <pattern>{"service":"${applicationName}","level":"%level","logger":"%logger{36}","thread":"%thread","message":"%message","correlationId":"%X{correlationId:-}","requestId":"%X{requestId:-}","idempotencyKey":"%X{idempotencyKey:-}"}</pattern>
                </pattern>
                <stackTrace/>
            </providers>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="console"/>
    </root>
</configuration>
